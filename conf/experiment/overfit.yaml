# @package _global_
# Quick convergence test on small dataset (CPU)

data:
  max_samples: 500

training:
  batch_size: 8
  max_epochs: 10
  accumulate_grad_batches: 2
  num_workers: 2
  accelerator: "cpu"

logging:
  run_name: "overfit-test"
